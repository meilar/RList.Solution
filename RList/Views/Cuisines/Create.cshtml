@{
  Layout = "_Layout";
}
@model RList.Models.Cuisine

<h1>Add a cuisine</h1>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <input type="submit" value="Add a new cuisine" />
}

<p>@Html.ActionLink("Show all cuisines", "Index")</p>